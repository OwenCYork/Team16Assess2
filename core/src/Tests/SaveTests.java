package Tests;

import com.mygdx.pirategame.Menu.Hud;
import com.mygdx.pirategame.Menu.LevelChoice;
import com.mygdx.pirategame.Menu.LoadScreen;
import com.mygdx.pirategame.Menu.SaveScreen;
import com.mygdx.pirategame.PirateGame;
import org.junit.Test;

import java.io.*;

import static org.junit.Assert.assertTrue;
import static org.mockito.Mockito.spy;


public class SaveTests {
/*
    private String Health;
    private String Score;
    private String Coins;
    private String HealthRe;
    private Boolean checker;

    @Test
    public void SavefileHealth() throws IOException {

        File file = new File("core\\assets\\saved.txt");
        InputStreamReader inputReader = new InputStreamReader(new FileInputStream(file),"UTF-8");
        BufferedReader bf = new BufferedReader(inputReader);
        Health = bf.readLine();

        checker = Health.isEmpty();
        assertTrue("Health exist in savefile.", checker);

    }
    @Test
    public void SavefileScore() throws IOException {


        Score = bf.readLine();
        checker = Score.isEmpty();
        assertTrue("Score exist in save file.", checker);

    }



*/
}
    

